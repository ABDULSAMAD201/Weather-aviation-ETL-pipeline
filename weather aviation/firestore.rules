rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all direct access - data should only be accessed through Cloud Functions
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
